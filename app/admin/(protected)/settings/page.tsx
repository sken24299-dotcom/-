import { Save } from 'lucide-react';
import { saveSiteSettingsAction } from '@/app/admin/cms-actions';
import { AdminNotice, AdminPageIntro, Field, adminButton, adminCard, adminInput, adminTextarea } from '@/components/admin/admin-ui';
import { getCmsConfig } from '@/lib/supabase/cms';

export const dynamic = 'force-dynamic';
export default async function SettingsPage({ searchParams }: { searchParams: Promise<{ status?: string }> }) { const [cms, params] = await Promise.all([getCmsConfig(), searchParams]); const s = cms.site; return <div><AdminPageIntro eyebrow="Site configuration" title="网站设置" description="维护品牌标识、联系方式、社交链接和页脚版权。" /><AdminNotice status={params.status} /><form action={saveSiteSettingsAction} className="space-y-4"><section className={adminCard}><div className="grid gap-4 sm:grid-cols-2"><Field label="网站名称"><input className={adminInput} name="siteName" defaultValue={s.siteName} required /></Field><Field label="联系邮箱"><input className={adminInput} type="email" name="email" defaultValue={s.email} required /></Field><Field label="Logo URL"><input className={adminInput} name="logoUrl" defaultValue={s.logoUrl} required /></Field><Field label="Favicon URL"><input className={adminInput} name="faviconUrl" defaultValue={s.faviconUrl} required /></Field><Field label="网站描述"><textarea className={adminTextarea} name="description" defaultValue={s.description} required /></Field><Field label="页脚版权"><textarea className={adminTextarea} name="copyright" defaultValue={s.copyright} required /></Field></div></section><section className={adminCard}><h3 className="text-base font-semibold">联系与社交</h3><div className="mt-5 grid gap-4 sm:grid-cols-2"><Field label="电话"><input className={adminInput} name="phone" defaultValue={s.phone} /></Field><Field label="地点"><input className={adminInput} name="location" defaultValue={s.location} /></Field><Field label="微信"><input className={adminInput} name="wechat" defaultValue={s.wechat} /></Field><Field label="WhatsApp"><input className={adminInput} name="whatsapp" defaultValue={s.whatsapp} /></Field><Field label="Telegram"><input className={adminInput} name="telegram" defaultValue={s.telegram} /></Field><Field label="GitHub"><input className={adminInput} name="github" defaultValue={s.github} /></Field><Field label="LinkedIn"><input className={adminInput} name="linkedin" defaultValue={s.linkedin} /></Field><Field label="Twitter / X"><input className={adminInput} name="twitter" defaultValue={s.twitter} /></Field></div></section><div className="flex justify-end"><button className={adminButton}><Save size={15} /> 保存网站设置</button></div></form></div>; }
